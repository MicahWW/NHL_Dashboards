@{
    Layout = "";
    string ConferenceName = (string)ViewData["Conference"]!;
    string TopDivisionName = string.Empty;
    string BotDivisionName = string.Empty;
    if (ConferenceName.Equals("Western"))
    {
        TopDivisionName = "Central";
        BotDivisionName = "Pacific";
    }
    else
    {
        TopDivisionName = "Atlantic";
        BotDivisionName = "Metropolitan";
    }
}

<!DOCTYPE html>

<html lang="en" xmlns="http://www.w3.org/1999/xhtml">

<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" type="text/css" href="/css/playGround.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Black+Ops+One&display=swap" rel="stylesheet">
    <script src="/js/resizeText.js"></script>
    <script src="/js/playGround.js"></script>
    <style id="dynamicStyle"></style>
</head>

<body>
    <div id="title">
        <img src="/images/@(ConferenceName)ConferenceLogo.png">
        <div class="resize-text-height">@ConferenceName Conference Standings</div>
    </div>

    <div id="playoff-teams" class="standing-section standing-section-vertical">
        @{
            string[] divs = ["top-division", "bot-division", "wildcards"];
            foreach (var div in divs)
            {
                int max = div != "wildcards" ? 3 : 2;
                <div id="@div" class="teams">
                    @for (int i = 1; i <= max; i++)
                    {
                        <div class="standing-row">
                            <div class="standing-number resize-text">@i</div>
                            <div id="@i" class="team-div">
                                <div class="team-info">
                                    <img class="logo" />
                                    <div class="clinch-indicator resize-text"></div>
                                    <div class="name resize-text"></div>
                                </div>
                                <div class="games-played resize-text"></div>
                                <div class="points resize-text"></div>
                            </div>
                        </div>
                    }
                </div>
            }
        }
    </div>
    <div id="outside-teams" class="standing-section standing-section-vertical teams">
        @for (int i = 9; i <= 16; i++)
        {
            <div class="standing-row">
                <div class="standing-number resize-text">@i</div>
                <div id="@i" class="team-div">
                    <div class="team-info">
                        <img class="logo" />
                        <div class="clinch-indicator resize-text"></div>
                        <div class="name resize-text"></div>
                    </div>
                    <div class="games-played resize-text"></div>
                    <div class="points resize-text"></div>
                </div>
            </div>
        }
    </div>

    <script>
        function resizeTeams() {
            let teamSections = document.getElementsByClassName("standing-section");
            if (window.innerHeight / window.innerWidth > 1) {
                for (let i = 0; i < teamSections.length; i++) {
                    teamSections[i].classList.replace("standing-section-horizontal", "standing-section-vertical");
                }
            } else {
                for (let i = 0; i < teamSections.length; i++) {
                    teamSections[i].classList.replace("standing-section-vertical", "standing-section-horizontal");
                }
            }
        }

        function resizeAll() {
            resizeTeams();
            resizeText();
            resizeTextToFitHeight();
        }

        const reloadData = async () => {
            console.log("loading data");
            await getData("@TopDivisionName", "@BotDivisionName", "@ConferenceName");
            resizeAll();
        };

        window.onload = reloadData;
        window.onresize = resizeAll;
        setInterval(reloadData, 1800000);
    </script>
</body>